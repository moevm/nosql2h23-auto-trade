name: Check scripts

on:
  push:
    branches:
      - "main"

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install-script
        run: $GITHUB_ACTION_PATH/install.sh
        shell: sh
